import { createConnection } from 'typeorm';

//Essa função busca pela projeto um arquivo ormconfig e pega as configurações estabelecidas lá, por isso não e necessario passar nenhum parametro.
createConnection();

